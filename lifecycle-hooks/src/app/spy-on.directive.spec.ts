import { SpyOnDirective } from './spy-on.directive';

describe('SpyOnDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyOnDirective();
    expect(directive).toBeTruthy();
  });
});
